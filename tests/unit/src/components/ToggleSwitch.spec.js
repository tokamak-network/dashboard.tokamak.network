import { shallowMount } from '@vue/test-utils';

import ToggleSwitch from '@/components/ToggleSwitch.vue';

describe('ToggleSwitch.vue', () => {
  let wrapper;

  beforeEach(() => {
    wrapper = shallowMount(ToggleSwitch);
  });

  it('', () => {});
});
