import { shallowMount } from '@vue/test-utils';

import BaseToggleSwitch from '@/components/BaseToggleSwitch.vue';

describe('BaseToggleSwitch.vue', () => {
  let wrapper;

  beforeEach(() => {
    wrapper = shallowMount(BaseToggleSwitch);
  });

  it('', () => {});
});
