import { shallowMount } from '@vue/test-utils';

import FooterContainer from '@/containers/FooterContainer.vue';

describe('FooterContainer.vue', () => {
  let wrapper;

  beforeEach(() => {
    wrapper = shallowMount(FooterContainer);
  });

  it('', () => {});
});
